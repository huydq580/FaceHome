import React, { Component } from 'react';
import {
    View,
    Text
} from 'react-native'

export default class TestScrollTabView extends Component {
    render () {
        return (
            <Text key={i}>{`tab${i}`}</Text>
        )
    }
}